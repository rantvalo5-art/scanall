# velas cacheadas (se regeneran solas desde la API)
.kline_cache/
.leadlag_T.csv

# velas diarias de todos los pares USDT que existieron (corrida 9)
.listados_cache/

# salidas de las corridas de graficos
rank_graficos_*.csv
potencia_graficos_*.csv
*.log

# muestreo de libro entre venues (corrida 10): ~470 MB de CSV crudo que se
# regenera con `dislocacion.py --muestrear`. El resultado vive en el preregistro.
.dislocacion/

# cache de las dos tablas de Farside (corrida 16) y de las cinco series de
# Yahoo (corrida 15). Se rebajan solas y no son el dato: son la copia local.
.etf_cache/
.macro_cache/
